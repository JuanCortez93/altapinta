# Neon Postgres — copiá la connection string desde el dashboard de Neon.
# Usá la "Pooled connection" y dejá el ?sslmode=require al final.
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxxx-pooler.REGION.aws.neon.tech/altapinta?sslmode=require"

# Auth (se usa a partir de la Fase 1). Generá uno con: openssl rand -base64 32
AUTH_SECRET=""
